"""Compatibility package for the requested ``backend/app`` layout.

Executable tests live in ``backend/tests`` so the same suite can run against
the installable ``filmgraph`` package and the compatibility import surface.
"""
